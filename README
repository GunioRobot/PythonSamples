Collection of various simple python examples, at the moment mainly about XML import and
export for the Magento eCommerce system.

Required libraries:

suds
magento
lxml

Configuration:

Rename Config.sample.py to Config.py and adjust settings. Never add a file to Git if it
contains login data!!!